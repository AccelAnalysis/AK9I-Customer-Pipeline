const PULSE_STEPS = ['Respondent','Operating pulse','Work & delays','Measurement & continuity','Department','Priorities','Review'];
const SUMMARY_STEPS = ['Meeting','Purpose & work','Ownership & systems','Handoffs & KPIs','Failure & risk','Actions & evidence','Review'];

function render() {
  if (!formType) return renderHome();
  if (confirmation) return renderConfirmation();
  if (pendingSubmission) return renderPending();
  const steps = formType === 'pulse' ? PULSE_STEPS : SUMMARY_STEPS;
  const content = formType === 'pulse' ? renderPulseStep() : renderSummaryStep();
  const pct = Math.round(((step + 1) / steps.length) * 100);
  document.title = `${steps[step]} — AK9I Department Fieldwork`;
  app.innerHTML = `<div class="progress-wrap"><div class="progress-meta"><span>Step ${step+1} of ${steps.length}</span><span>${pct}%</span></div><div class="progress-track" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${pct}"><div class="progress-fill" style="width:${pct}%"></div></div></div>
    ${errorMessages.length ? `<div class="error-summary" role="alert"><strong>Please complete the highlighted requirements.</strong><ul>${errorMessages.map(e=>`<li>${esc(e)}</li>`).join('')}</ul></div>` : ''}
    ${content}
    <div class="status-line"><span class="dot ${navigator.onLine?'online':'offline'}"></span><span>${navigator.onLine?'Online':'Offline — your draft remains on this device'}</span>${sessionToken?'':'<span> · Submission token not present in this link</span>'}</div>
    <nav class="nav-bar" aria-label="Form navigation"><div class="nav-inner">${step>0?'<button class="button secondary" id="back-btn" type="button">Back</button>':'<button class="button secondary" id="home-btn" type="button">Exit</button>'}<button class="button" id="continue-btn" type="button" ${submitting?'disabled':''}>${step===steps.length-1?(submitting?'Submitting…':'Submit response'):'Continue'}</button></div></nav>`;
  bindInputs();
  document.getElementById('back-btn')?.addEventListener('click', () => { errorMessages=[]; step--; window.scrollTo({top:0,behavior:'smooth'}); render(); });
  document.getElementById('home-btn')?.addEventListener('click', () => { window.location.href = window.location.pathname; });
  document.getElementById('continue-btn')?.addEventListener('click', handleContinue);
  document.getElementById('clear-draft')?.addEventListener('click', () => { if (window.confirm('Clear the saved draft on this device?')) { clearDrafts(); answers = defaultAnswers(); applyPrefills(); render(); } });
}

function renderPulseStep() {
  const dept = answers.pulse.department;
  switch (step) {
    case 0: return `<section class="step-card"><p class="step-kicker">Individual Department Operating Pulse</p><h2>Respondent identification</h2>${introNotice()}${fieldText('pulse.email','Work email',{required:true,type:'email',maxLength:180,hint:'Self-identified; this does not verify identity.'})}${fieldText('pulse.name','Name (optional)',{maxLength:120})}${departmentSelector()}${fieldText('pulse.role','Role or job title',{required:true,maxLength:140})}${fieldRadio('pulse.tenure','Time with AK9I',TENURE,{required:true})}${fieldRadio('pulse.perspective','Perspective represented',PERSPECTIVE,{required:true})}${fieldChecks('pulse.otherDepartments','Other departments supported',Object.values(DEPARTMENTS).filter(x=>x!==DEPARTMENTS[dept]),{})}<button class="button ghost" id="clear-draft" type="button">Clear saved draft</button></section>`;
    case 1: return `<section class="step-card"><p class="step-kicker">Rapid operating pulse</p><h2>How well is the current operating system working?</h2><p class="hint">Tap one response for every statement.</p>${ratingCards('pulse.rapid',RAPID_STATEMENTS)}</section>`;
    case 2: {
      const tasks = DEPT[dept]?.tasks || ['Daily operations','Customer/student support','Scheduling','Reporting','Purchasing','Records','Coordination with other departments','Other'];
      return `<section class="step-card"><p class="step-kicker">Work, delays, systems & accountability</p><h2>How work moves today</h2>${fieldChecks('pulse.tasks','Select up to five activities that make up most of your normal work.',tasks,{required:true,max:5,hint:'Choose the work you actually perform most often.'})}${fieldRadio('pulse.outsideRole','Approximately how much of your working time is spent on tasks outside your primary role?',['0–10%','11–25%','26–50%','51–75%','More than 75%','Not sure'],{required:true})}${fieldRadio('pulse.cracks','How often do important tasks fall through the cracks or require last-minute recovery?',['Never','Less than monthly','Monthly','Weekly','Daily or almost daily','Not sure'],{required:true})}${fieldRadio('pulse.waiting','How often is your work delayed while waiting for another person or department?',['Never','Less than monthly','Monthly','Weekly','Daily or almost daily','Not sure'],{required:true})}${fieldChecks('pulse.delayCauses','Select up to three common causes of delay.',DELAY_CAUSES,{required:true,max:3})}${fieldChecks('pulse.systems','Which systems or methods do you use regularly?',SYSTEMS,{required:true})}${fieldText('pulse.friction','Which system or process creates the most friction? (optional)',{maxLength:500})}</section>`;
    }
    case 3: return `<section class="step-card"><p class="step-kicker">Measurement, waste & continuity</p><h2>What is visible, vulnerable, or being lost?</h2>${fieldChecks('pulse.currentMeasures','What is currently measured?',CURRENT_MEASURES,{required:true})}${fieldChecks('pulse.desiredMeasures','What should be measured? Select up to three.',DESIRED_MEASURES,{required:true,max:3})}${fieldChecks('pulse.waste','Where is time or money being wasted? Select up to three.',WASTE,{required:true,max:3})}${fieldRadio('pulse.singleDependency','Would an important function stop or become severely disrupted if one person were unexpectedly absent?',['Yes','No','Not sure'],{required:true,rerender:true})}${get('pulse.singleDependency')==='Yes'?fieldText('pulse.dependencyFunction','Identify the function or responsibility — not the individual person.',{required:true,maxLength:500}):''}${fieldRadio('pulse.immediateRisk','Are you aware of an immediate risk that could affect AK9I’s ability to meet current commitments?',['Yes','No','Not sure'],{required:true,rerender:true})}${get('pulse.immediateRisk')==='Yes'?`${fieldChecks('pulse.riskAreas','Select one or more risk categories.',RISKS,{required:true})}${fieldRadio('pulse.riskUrgency','How urgent is the risk?',['Requires attention today','Within 2–7 days','Within 8–30 days','Longer-term','Not sure'],{required:true})}<div class="notice warning">Do not enter sensitive details in this form. Use Private follow-up at the end if needed.</div>`:''}</section>`;
    case 4: return renderDepartmentSection(dept);
    case 5: return `<section class="step-card"><p class="step-kicker">Final section</p><h2>Priorities and a seven-day move</h2>${fieldChecks('pulse.priorities','Select the three areas AK9I should address first.',PRIORITIES,{required:true,max:3,hint:'Select exactly three.'})}${fieldTextarea('pulse.sevenDay','What is one practical improvement that could be started or completed within seven days?',{required:true,maxLength:1200})}${fieldSelect('pulse.stopStartContinue','Choose one',['AK9I should stop…','AK9I should start…','AK9I should continue…'],{required:true})}${fieldText('pulse.stopStartText','Optional detail',{maxLength:700})}${fieldText('pulse.evidenceSource','What document, report, system, or record should the transformation team inspect to verify what is actually happening? (optional)',{maxLength:800})}${fieldRadio('pulse.willingness','Would you be willing to help implement the seven-day improvement?',['I would be willing to help implement the seven-day improvement.','Maybe, depending on what is required.','No.','Not applicable.'],{required:true})}${fieldRadio('pulse.privateFollowup','Would you like a private follow-up from the Transformation Lead?',['Yes','No'],{required:true})}<div class="notice info">No sensitive explanation is collected here. Your work email provides the follow-up method.</div></section>`;
    case 6: return renderReview('pulse');
  }
}
function departmentSelector() {
  const current = answers.pulse.department;
  if (initialDepartment) return `<div class="field"><span class="field-label required">Primary department</span><div class="prefilled-value">${esc(DEPARTMENTS[initialDepartment])}</div><small>Selected by this meeting link.</small></div>`;
  return `<fieldset class="field"><legend class="required">Primary department</legend><div class="choice-grid">${Object.entries(DEPARTMENTS).map(([k,v])=>`<label class="choice"><input type="radio" name="pulse-department" data-department-field="pulse.department" value="${k}" ${current===k?'checked':''}><span>${esc(v)}</span></label>`).join('')}</div></fieldset>`;
}
function renderDepartmentSection(dept) {
  const d = DEPT[dept];
  if (!d) return `<section class="step-card"><p class="step-kicker">Department-specific section</p><h2>${esc(DEPARTMENTS[dept] || 'Department')}</h2><div class="notice info">No specialized question set is defined for this department in Tuesday’s four-meeting fieldwork. Continue to the final priorities section.</div>${fieldTextarea('pulse.deptImprovement','What is the single highest-impact department improvement that could begin within seven days?',{required:true})}</section>`;
  return `<section class="step-card"><p class="step-kicker">Department-specific section</p><h2>${esc(DEPARTMENTS[dept])}</h2>${ratingCards('pulse.deptRatings',d.ratings)}${fieldRadio('pulse.constraint',d.constraintLabel || 'Most urgent constraint',d.constraint,{required:true})}${d.extraChoice?fieldRadio('pulse.deptExtra',d.extraChoice.label,d.extraChoice.options,{required:true}):''}${d.extra?fieldTextarea('pulse.professionalDevelopment',d.extra,{required:false,maxLength:900}):''}${fieldTextarea('pulse.deptImprovement',d.improvement,{required:true,maxLength:1200})}</section>`;
}

function renderSummaryStep() {
  switch (step) {
    case 0: return `<section class="step-card"><p class="step-kicker">Department Meeting Summary</p><h2>Meeting identification</h2><div class="notice info">Complete this once after the department meeting. Use role titles rather than naming individuals whenever possible.</div>${fieldText('summary.facilitatorEmail','Facilitator email',{required:true,type:'email',maxLength:180})}${summaryDepartmentSelector()}${fieldText('summary.meetingDate','Meeting date',{required:true,type:'date'})}${fieldText('summary.startTime','Meeting start time',{required:true,type:'time'})}${fieldText('summary.endTime','Meeting end time',{required:true,type:'time'})}<div class="grid-2">${fieldNumber('summary.attendees','Number of attendees',{required:true,min:1,max:500})}${fieldNumber('summary.responseCount','Response count received',{required:true,min:0,max:500})}</div>${fieldText('summary.sessionDisplay','Form session ID',{readonly:true,hint:'Selected by the meeting link.'})}</section>`;
    case 1: return `<section class="step-card"><p class="step-kicker">Department operating map</p><h2>Purpose and recurring work</h2>${fieldTextarea('summary.purpose','State the department’s agreed purpose in one sentence.',{required:true,maxLength:800})}<fieldset class="field"><legend class="required">Five most important recurring work activities</legend>${answers.summary.recurringWork.map((v,i)=>fieldText(`summary.recurringWork.${i}`,`Activity ${i+1}`,{required:true,maxLength:300})).join('')}</fieldset></section>`;
    case 2: return `<section class="step-card"><p class="step-kicker">Department operating map</p><h2>Ownership and systems</h2>${fieldText('summary.accountableRole','Which role is accountable for the department’s primary outcomes?',{required:true,maxLength:300})}${fieldRadio('summary.ownershipClarity','Is that ownership clear?',['Yes','Partly','No'],{required:true})}${fieldTextarea('summary.overlapping','Which responsibilities are overlapping?',{maxLength:1200,hint:'Use role titles rather than naming individuals whenever possible.'})}${fieldTextarea('summary.orphaned','Which responsibilities are orphaned?',{maxLength:1200,hint:'Describe work with no clear owner.'})}${fieldChecks('summary.systems','Which systems actually contain the department’s work?',SUMMARY_SYSTEMS,{required:true})}${fieldText('summary.authoritativeSource','Identify the authoritative source, if known.',{maxLength:700})}</section>`;
    case 3: return `<section class="step-card"><p class="step-kicker">Department operating map</p><h2>Dependencies, handoffs, and KPIs</h2><h3>Dependencies and handoffs</h3>${answers.summary.handoffs.map((_,i)=>repeatHandoff(i)).join('')}<h3>KPIs</h3>${fieldTextarea('summary.currentMeasured','What is currently measured?',{maxLength:1200})}<p class="hint">Capture up to three priority KPIs that should be measured.</p>${answers.summary.kpis.map((_,i)=>repeatKpi(i)).join('')}</section>`;
    case 4: return `<section class="step-card"><p class="step-kicker">Department operating map</p><h2>Failure points and immediate risk</h2><p class="hint">Record the three most material failure points.</p>${answers.summary.failures.map((_,i)=>repeatFailure(i)).join('')}<h3>Immediate risk status</h3><p class="hint">Use GREEN, YELLOW, RED, or UNKNOWN for every category.</p><div class="risk-grid">${RISK_AREAS.map(area=>riskRow(area)).join('')}</div></section>`;
    case 5: return `<section class="step-card"><p class="step-kicker">Department operating map</p><h2>Immediate fixes and evidence requests</h2><p class="hint">Agree one or two low-regret immediate fixes. The first action is required.</p>${answers.summary.fixes.map((_,i)=>repeatFix(i)).join('')}<h3>Evidence requests</h3>${answers.summary.evidenceRequests.map((_,i)=>repeatEvidence(i)).join('')}${fieldTextarea('summary.unresolved','Contradictions or unresolved questions',{maxLength:1800,hint:'Do not enter confidential allegations.'})}${fieldRadio('summary.escalation','Private escalation required',['No','Yes — financial/control','Yes — HR/personnel','Yes — student continuity','Yes — safety/canine welfare','Yes — legal/compliance','Yes — data/security','Other'],{required:true})}</section>`;
    case 6: return renderReview('summary');
  }
}
function summaryDepartmentSelector() {
  const current = answers.summary.department;
  if (initialDepartment) return `<div class="field"><span class="field-label required">Department</span><div class="prefilled-value">${esc(DEPARTMENTS[initialDepartment])}</div><small>Selected by this meeting link.</small></div>`;
  return `<fieldset class="field"><legend class="required">Department</legend><div class="choice-grid">${TUESDAY_DEPARTMENTS.map(k=>`<label class="choice"><input type="radio" name="summary-department" data-department-field="summary.department" value="${k}" ${current===k?'checked':''}><span>${esc(DEPARTMENTS[k])}</span></label>`).join('')}</div></fieldset>`;
}
function repeatHandoff(i) { const p=`summary.handoffs.${i}`; return `<div class="repeat-card"><h3>Handoff ${i+1}</h3>${fieldText(`${p}.dependency`,'Dependency or handoff',{maxLength:500})}${fieldText(`${p}.sending`,'Sending role/department',{maxLength:300})}${fieldText(`${p}.receiving`,'Receiving role/department',{maxLength:300})}${fieldTextarea(`${p}.failure`,'Current failure',{maxLength:900})}</div>`; }
function repeatKpi(i) { const p=`summary.kpis.${i}`; return `<div class="repeat-card"><h3>KPI ${i+1}</h3>${fieldText(`${p}.metric`,'What should be measured?',{maxLength:500})}${fieldText(`${p}.owner`,'Who should own the KPI?',{maxLength:300})}${fieldText(`${p}.source`,'What source would support it?',{maxLength:500})}</div>`; }
function repeatFailure(i) { const p=`summary.failures.${i}`; return `<div class="repeat-card"><h3>Failure point ${i+1}</h3>${fieldText(`${p}.point`,'Failure point',{required:true,maxLength:600})}${fieldText(`${p}.frequency`,'Frequency',{required:true,maxLength:200})}${fieldText(`${p}.impact`,'Impact',{required:true,maxLength:500})}${fieldText(`${p}.notices`,'Who notices it? (role)',{required:true,maxLength:300})}${fieldTextarea(`${p}.response`,'Current response',{required:true,maxLength:800})}</div>`; }
function riskRow(area) { const path=`summary.risks.${area}`; const value=get(path)||''; return `<div class="risk-row"><strong>${esc(area)}</strong><div class="risk-options">${['GREEN','YELLOW','RED','UNKNOWN'].map(v=>`<label class="risk-pill"><input type="radio" name="${id(path)}" data-field="${esc(path)}" value="${v}" ${value===v?'checked':''}><span>${v}</span></label>`).join('')}</div></div>`; }
function repeatFix(i) { const p=`summary.fixes.${i}`; const required=i===0; return `<div class="repeat-card"><h3>Immediate fix ${i+1}</h3>${fieldText(`${p}.action`,'Action',{required,maxLength:700})}${fieldText(`${p}.owner`,'Owner role',{required,maxLength:300})}${fieldText(`${p}.due`,'Due date',{required,type:'date'})}${fieldRadio(`${p}.priority`,'Priority',['P0 — immediate','P1 — this week','P2 — within 30 days'],{required})}${fieldText(`${p}.evidence`,'Evidence of completion',{required,maxLength:700})}</div>`; }
function repeatEvidence(i) { const p=`summary.evidenceRequests.${i}`; return `<div class="repeat-card"><h3>Evidence request ${i+1}</h3>${fieldText(`${p}.needed`,'Evidence needed',{maxLength:700})}${fieldText(`${p}.source`,'Source/system',{maxLength:400})}${fieldText(`${p}.role`,'Responsible role',{maxLength:300})}${fieldText(`${p}.due`,'Due date',{type:'date'})}</div>`; }

function renderReview(type) {
  const email = type==='pulse' ? answers.pulse.email : answers.summary.facilitatorEmail;
  const dept = type==='pulse' ? answers.pulse.department : answers.summary.department;
  const sections = type==='pulse' ? pulseReviewSections() : summaryReviewSections();
  return `<section class="step-card"><p class="step-kicker">Final review</p><h2>Review before submission</h2><div class="notice info"><strong>Email:</strong> ${esc(email)}<br><strong>Department:</strong> ${esc(DEPARTMENTS[dept] || dept)}<br><strong>Session:</strong> ${esc(sessionId || 'Not selected')}</div>${!sessionToken?'<div class="notice warning"><strong>This link does not include a meeting-session token.</strong> You can review your answers, but submission will be rejected until you open the distributed meeting link or QR code.</div>':''}${sections}<div class="notice info">Submitting again from the same email, session, department, and form type creates a new revision and marks the latest response current.</div></section>`;
}
function reviewRows(rows) { return rows.map(([k,v])=>`<dl class="review-row"><dt>${esc(k)}</dt><dd>${esc(formatValue(v))}</dd></dl>`).join(''); }
function formatValue(v) { if (Array.isArray(v)) return v.filter(Boolean).join('; ') || '—'; if (v && typeof v==='object') return Object.entries(v).map(([k,x])=>`${k}: ${formatValue(x)}`).join(' | '); return hasValue(v)?String(v):'—'; }
function pulseReviewSections() {
  const p=answers.pulse; return `<div class="review-section"><h3>Identity</h3>${reviewRows([['Role',p.role],['Tenure',p.tenure],['Perspective',p.perspective]])}</div><div class="review-section"><h3>Operating pulse</h3>${reviewRows(RAPID_STATEMENTS.map((s,i)=>[`${i+1}. ${s}`,p.rapid[i+1]]))}</div><div class="review-section"><h3>Work and systems</h3>${reviewRows([['Regular work',p.tasks],['Time outside role',p.outsideRole],['Tasks fall through',p.cracks],['Waiting on others',p.waiting],['Delay causes',p.delayCauses],['Systems',p.systems],['Friction',p.friction]])}</div><div class="review-section"><h3>Continuity and priorities</h3>${reviewRows([['Current measures',p.currentMeasures],['Desired measures',p.desiredMeasures],['Waste',p.waste],['Single-person dependency',p.singleDependency],['Dependency function',p.dependencyFunction],['Immediate risk',p.immediateRisk],['Risk areas',p.riskAreas],['Urgency',p.riskUrgency],['Department constraint',p.constraint],['Department improvement',p.deptImprovement],['Top priorities',p.priorities],['Seven-day improvement',p.sevenDay],['Stop/start/continue',`${p.stopStartContinue} ${p.stopStartText}`],['Evidence source',p.evidenceSource],['Willingness',p.willingness],['Private follow-up',p.privateFollowup]])}</div>`; }
function summaryReviewSections() {
  const s=answers.summary; return `<div class="review-section"><h3>Meeting</h3>${reviewRows([['Date',s.meetingDate],['Time',`${s.startTime}–${s.endTime}`],['Attendees',s.attendees],['Responses',s.responseCount],['Purpose',s.purpose],['Recurring work',s.recurringWork]])}</div><div class="review-section"><h3>Operating map</h3>${reviewRows([['Accountable role',s.accountableRole],['Ownership clarity',s.ownershipClarity],['Overlapping responsibilities',s.overlapping],['Orphaned responsibilities',s.orphaned],['Systems',s.systems],['Authoritative source',s.authoritativeSource],['Current measured',s.currentMeasured]])}</div><div class="review-section"><h3>Handoffs and KPIs</h3>${reviewRows([['Handoffs',s.handoffs.filter(x=>Object.values(x).some(Boolean))],['KPIs',s.kpis.filter(x=>Object.values(x).some(Boolean))]])}</div><div class="review-section"><h3>Failures, risk, actions</h3>${reviewRows([['Failure points',s.failures],['Risk status',s.risks],['Immediate fixes',s.fixes.filter(x=>Object.values(x).some(Boolean))],['Evidence requests',s.evidenceRequests.filter(x=>Object.values(x).some(Boolean))],['Unresolved questions',s.unresolved],['Private escalation',s.escalation]])}</div>`; }

function bindInputs() {
  app.querySelectorAll('[data-field]').forEach(el => {
    const handler = () => {
      const path = el.dataset.field;
      if (el.type === 'checkbox') {
        const checked = [...app.querySelectorAll(`input[type="checkbox"][data-field="${cssEscape(path)}"]:checked`)].map(x=>x.value);
        const max = Number(el.dataset.max || 0);
        if (max && checked.length > max) { el.checked=false; showTransient(`Select no more than ${max}.`); return; }
        set(path, [...app.querySelectorAll(`input[type="checkbox"][data-field="${cssEscape(path)}"]:checked`)].map(x=>x.value));
      } else set(path, el.value);
      const emailPath = formType==='pulse'?'pulse.email':'summary.facilitatorEmail';
      if (path===emailPath && /^\S+@\S+\.\S+$/.test(el.value.trim())) localStorage.setItem('ak9i-field-email',el.value.trim());
      queueDraftSave();
      if (el.dataset.rerender) render();
    };
    el.addEventListener(el.type==='radio'||el.type==='checkbox'||el.tagName==='SELECT'?'change':'input', handler);
  });
  app.querySelectorAll('[data-department-field]').forEach(el => el.addEventListener('change', () => {
    set(el.dataset.departmentField,el.value); queueDraftSave(); render();
  }));
}
function cssEscape(v) { return window.CSS?.escape ? CSS.escape(v) : v.replace(/["\\]/g,'\\$&'); }
function showTransient(message) {
  const node=document.createElement('div'); node.className='notice warning'; node.setAttribute('role','status'); node.textContent=message; app.prepend(node); setTimeout(()=>node.remove(),2200);
}
